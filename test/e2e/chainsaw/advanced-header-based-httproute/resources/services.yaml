apiVersion: v1
kind: Service
metadata:
  name: advanced-header-based-httproute-stable-service
spec:
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: advanced-header-based-httproute
---
apiVersion: v1
kind: Service
metadata:
  name: advanced-header-based-httproute-canary-service
spec:
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: advanced-header-based-httproute
