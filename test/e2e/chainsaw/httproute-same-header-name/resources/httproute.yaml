apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: httproute-same-header-name
  namespace: default
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: default
  rules:
    - backendRefs:
        - name: httproute-same-header-name-stable-service
          port: 80
        - name: httproute-same-header-name-canary-service
          port: 80
