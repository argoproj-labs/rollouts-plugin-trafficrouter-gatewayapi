apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: grpcroute-rollout
  namespace: default
spec:
  replicas: 2
  strategy:
    canary:
      canaryService: single-grpcroute-canary-service
      stableService: single-grpcroute-stable-service
      trafficRouting:
        plugins:
          argoproj-labs/gatewayAPI:
            grpcRoute: grpcroute-basic
            namespace: default
      steps:
        - setWeight: 30
        - pause: {duration: 30s}
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: single-grpcroute
  template:
    metadata:
      labels:
        app: single-grpcroute
    spec:
      containers:
        - name: rollouts-demo
          image: argoproj/rollouts-demo:red
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
