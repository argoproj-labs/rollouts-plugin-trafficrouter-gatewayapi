apiVersion: gateway.networking.k8s.io/v1
kind: GRPCRoute
metadata:
  name: grpcroute-basic
  namespace: default
spec:
  parentRefs:
    - name: traefik-gateway
  rules:
    - backendRefs:
        - name: single-grpcroute-stable-service
          port: 80
        - name: single-grpcroute-canary-service
          port: 80
